{
  "name": "xray-evidence-analyzer",
  "version": "0.1.0",
  "description": "Analyze X-Ray traces and generate a CSV of no-evidence test runs.",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "analyze": "npm run build && node dist/index.js",
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "dependencies": {
    "csv-writer": "^1.6.0",
    "dotenv": "^16.1.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
